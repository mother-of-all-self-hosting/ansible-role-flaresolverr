# SPDX-FileCopyrightText: 2023 Slavi Pantaleev
# SPDX-FileCopyrightText: 2025 sudo-Tiz
# SPDX-FileCopyrightText: 2025, 2026 Suguru Hirahara
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
- name: Perform FlareSolverr installation tasks
  when: flaresolverr_enabled | bool
  tags:
    - setup-all
    - setup-flaresolverr
    - install-all
    - install-flaresolverr
  block:
    - name: Validate FlareSolverr configuration
      ansible.builtin.include_tasks: "{{ role_path }}/tasks/validate_config.yml"
    - name: Install FlareSolverr
      ansible.builtin.include_tasks: "{{ role_path }}/tasks/install.yml"

- name: Perform FlareSolverr uninstallation tasks
  when: not flaresolverr_enabled | bool
  tags:
    - setup-all
    - setup-flaresolverr
  block:
    - name: Uninstall FlareSolverr
      ansible.builtin.include_tasks: "{{ role_path }}/tasks/uninstall.yml"
